# 变更记录

## 0.2.3 · 2026-09-23

- 最终 Windows CI 运行 35932215812 成功，提交 a2bdb280224edcb1dae5a085626251eebe795527 产出三个测试安装器；96+28+19 项检查及安装启动/共存卸载流程通过。
- 修复共享类库独立部署参数、Inno 引擎版本识别；固定实际 .NET SDK 为 10.0.100。
- 补齐三个应用的 App.xaml、生成的 XAML 元数据和 InitializeComponent，修复控件资源加载时的启动崩溃。保留自定义单实例入口。
- 增加仅测试环境启用的启动诊断及打包前窗口检查。以下条目记录此前准备阶段。

- 准备哲里设置、课表、哲喵三个独立 Inno 安装器；后两者内置公共组件前置安装器。
- 公共组件由哲里设置拥有，依赖未移除时阻止卸载；保留业务数据，拒绝不同构建混装。
- 发布使用独立暂存目录、载荷完整性与 SHA-256 检查，收集依赖许可；六个入口增加运行状态 mutex。
- 固定 Inno 6.5.4 下载校验和，保留简体中文翻译及许可来源。
- GitHub Windows 流程完成原生发布后编译安装器，再验证安装、启动、共存卸载和数据保留；全部通过才上传安装包。
- 20 项 PowerShell 语法及合成载荷检查通过。Windows 流程尚未执行，本版本尚无可交付 EXE。

## 0.2.2 · 2026-09-23

- 桌宠记住显示器内相对位置与隐藏状态；使用物理像素移动，处理 DPI、工作区变化及显示器断开，托盘可移回主屏。
- 采用可取消的鼠标手势状态，超过系统拖动阈值后不再被解释为点击；松手位置也会检查阈值。
- 在临时矢量角色上加入眨眼、耳朵和尾巴动作；隐藏、拖动、减少动态效果、远程会话和软件渲染时暂停动画。
- 全屏避让仅作用于同一显示器；显示检查与设置请求分离，服务离线不阻塞避让。
- 新增 20 项位置、跨屏、缩放、手势及持久化测试。未将托管编译视为 Windows 多屏或视觉验收。

## 0.2.1 · 2026-09-23

- 用生产客户端完成真实模型列表、合成课程问答和合成文件来源问答测试；密钥未入包。
- 区分主动取消与响应超时，修复超时被显示成“已停止”的问题；等待时限覆盖响应正文。
- 分类处理密钥、余额、限流、参数、模型和服务错误；不把远程错误正文写入会话。
- 增加输出上限、响应体大小限制、截断提示和响应结构校验，无自动重试。
- 增加独立客户端测试项目及 Windows 复测脚本；真实测试仅显式启用。

## 0.2.0 · 2026-09-23

- 哲里设置新增文件知识库页面，管理持续读取、云端选段与私密目录授权。
- 哲喵增加PDF、DOCX、XLSX、PPTX文字提取，保留TXT和Markdown支持。
- 增加本地SQLite持久索引、运行期间每分钟增量检查、查询前哈希核对、删除/撤权清理。
- 增加来源位置、提取不完整提示、查看所在目录，以及最多6个用户选定片段的DeepSeek预览发送。
- 发送前复查目录授权和文件版本；私密子目录优先；摘录与其派生回答不进入之后的云端历史。
- 增加27项回归测试，核心测试由52项增至79项；Windows跳过3项Linux符号链接测试。
- 延续0.1.0的课表、主题、备份、回收站、单次变动与桌宠功能。尚未提供Windows实机验收或正式安装包。

## 0.1.0 · 2026-09-22

建立三应用原生框架、共享组件、独立数据库所有者、课表领域规则、单次变动、回收站、草稿、加密备份、哲喵会话及基础文本搜索。52项核心测试通过；完整发行门槛记录于STATUS.md。
